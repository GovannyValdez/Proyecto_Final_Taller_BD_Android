<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    android:padding="24dp">

    <!-- Título -->
    <TextView
        android:id="@+id/tvTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Modelos de Avión"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="#1976D2"
        android:layout_marginTop="60dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Subtítulo -->
    <TextView
        android:id="@+id/tvSubtitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gestión de modelos de avión"
        android:textSize="16sp"
        android:textColor="#666666"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitulo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botón Agregar Modelo -->
    <Button
        android:id="@+id/btnAgregarModelo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Agregar Modelo"
        android:textSize="18sp"
        android:paddingVertical="16dp"
        android:backgroundTint="#4CAF50"
        android:layout_marginTop="60dp"
        app:cornerRadius="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvSubtitulo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botón Consultar Modelo -->
    <Button
        android:id="@+id/btnConsultarModelo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Consultar Modelo"
        android:textSize="18sp"
        android:paddingVertical="16dp"
        android:backgroundTint="#1976D2"
        android:layout_marginTop="20dp"
        app:cornerRadius="12dp"
        app:layout_constraintTop_toBottomOf="@id/btnAgregarModelo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botón Modificar Modelo -->
    <Button
        android:id="@+id/btnModificarModelo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Modificar Modelo"
        android:textSize="18sp"
        android:paddingVertical="16dp"
        android:backgroundTint="#FF9800"
        android:layout_marginTop="20dp"
        app:cornerRadius="12dp"
        app:layout_constraintTop_toBottomOf="@id/btnConsultarModelo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botón Eliminar Modelo -->
    <Button
        android:id="@+id/btnEliminarModelo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Eliminar Modelo"
        android:textSize="18sp"
        android:paddingVertical="16dp"
        android:backgroundTint="#D32F2F"
        android:layout_marginTop="20dp"
        app:cornerRadius="12dp"
        app:layout_constraintTop_toBottomOf="@id/btnModificarModelo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Línea separadora -->
    <View
        android:id="@+id/lineaSeparadora"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="#CCCCCC"
        android:layout_marginTop="40dp"
        app:layout_constraintTop_toBottomOf="@id/btnEliminarModelo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botón Regresar al Menú -->
    <Button
        android:id="@+id/btnRegresar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Regresar al Menú"
        android:textSize="16sp"
        android:paddingVertical="14dp"
        android:backgroundTint="#757575"
        android:layout_marginTop="24dp"
        app:cornerRadius="12dp"
        app:layout_constraintTop_toBottomOf="@id/lineaSeparadora"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>